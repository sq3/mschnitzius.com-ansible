kasten.beer's Network Ansibel Repository
========================================

This repository manages the inventory of the kasten.beer network.

# Usage
First every playbook needs a ansible.cfg with the following variables

	inventory = ../../hosts/
	roles_path = ../../roles/
	log_path = ../../logs/
